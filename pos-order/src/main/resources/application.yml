spring:
  application:
    name: pos-order-service
  main:
    allow-circular-references: false
server:
  port: 9000
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
